/* eslint-disable @next/next/no-html-link-for-pages */
import React from "react";

const Login = () => {
  return <a href="/api/auth/login"><button className='btn btn-ghost'>Log In</button></a>;
};

export default Login;